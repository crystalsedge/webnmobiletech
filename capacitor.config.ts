import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'file-explorer-local',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
